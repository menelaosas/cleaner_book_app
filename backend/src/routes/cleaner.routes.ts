import { Router } from 'express';

const router = Router();

// Placeholder routes - implement later
router.get('/', (req, res) => {
  res.json({ success: true, message: 'Cleaner routes - coming soon' });
});

export default router;
